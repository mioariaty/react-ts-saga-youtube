export const videoSelector = (state: AppState) => state.videoReducer;
export const videoSearchSelector = (state: AppState) => state.videoSearchReducer;
export const commentSelector = (state: AppState) => state.comments;
export const channelSelector = (state: AppState) => state.channelReducer;
export const nightModeSelector = (state: AppState) => state.toggleReducer;
