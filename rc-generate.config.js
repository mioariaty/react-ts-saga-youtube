const styles = `
  @import 'styles/abstracts/abstracts.scss'
`;
const actions = ``;
const reducers = ``;
const sagas = ``;
const thunks = ``;

const config = {
  baseUrl: 'src',
  typescript: true,
  reactNative: false,
  templates: {
    styles,
    actions,
    reducers,
    sagas,
    thunks,
  },
};
module.exports = config;
